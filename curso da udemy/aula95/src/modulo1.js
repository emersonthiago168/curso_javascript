const nome = 'Emerson';
const sobrenome = 'Thiago';
const idade = 20;
const cpf = '23123255555'

function soma(x, y) {
    return x + y;
}

export default (x, y) => x * y;

export { nome, sobrenome, idade, soma };